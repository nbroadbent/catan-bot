{
  "name": "catan-copilot",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Firefox extension that overlays colonist.io with live card counting, balanced-dice tracking, and strategy advice",
  "scripts": {
    "build": "vite build && vite build --config vite.inject.config.ts",
    "test": "vitest run",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "playwright": "^1.62.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
